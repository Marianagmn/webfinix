export const environment = {
  production: true,
  apiUrl: 'https://api.your-production-domain.com', // Replace with actual production backend URL (already includes /api)
  useProxy: false // In production, make direct requests to backend (no proxy)
};
