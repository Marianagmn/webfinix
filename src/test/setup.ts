// src/test/setup.ts
// Test setup file for Vitest
import { expect, afterEach } from 'vitest';

// Cleanup after each test
afterEach(() => {
  // cleanup() removed - @testing-library/angular not installed
});
